<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Hệ thống quản lý thư viện</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <style>
        /* ===============================
           TONE MÀU ĐỒNG BỘ TOÀN WEBSITE
           =============================== */
        :root {
            --main-dark: #004a99;
            --main-light: #00a2ff;
            --footer-dark: #003b78;
            --footer-light: #006edc;
        }

        /* NAVBAR với gradient xanh dương */
        .navbar-custom {
            background: linear-gradient(90deg, var(--main-dark), var(--main-light));
        }

        .navbar-brand img {
            height: 45px;
            width: auto;
        }

        /* Style link menu */
        .navbar-nav .nav-link {
            color: white !important;
            font-weight: 600;
            transition: 0.25s;
        }

            .navbar-nav .nav-link:hover {
                color: #e6f7ff !important;
                text-shadow: 0 0 6px #fff;
            }

        /* Thanh tìm kiếm */
        .search-box {
            display: flex;
            align-items: center;
            width: 350px;
            background: #ffffff;
            border-radius: 20px;
            padding: 5px 12px;
            border: 1px solid #ccc;
        }

            .search-box input {
                border: none;
                outline: none;
                width: 100%;
                background: transparent;
            }

            .search-box i {
                color: #555;
                font-size: 18px;
                margin-right: 8px;
            }

        /* FOOTER dùng tông xanh đồng bộ */
        footer {
            background: linear-gradient(90deg, var(--footer-dark), var(--footer-light));
        }

            footer h5 {
                color: #ffffff;
                font-weight: bold;
            }

            footer p, footer a {
                color: #dbe9ff !important;
            }

                footer a:hover {
                    color: #ffffff !important;
                    text-decoration: underline;
                }

        .social-links a {
            transition: 0.25s;
        }

            .social-links a:hover {
                color: #00c8ff !important;
                transform: scale(1.1);
            }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom shadow-sm">
        <div class="container">

            <a class="navbar-brand d-flex align-items-center" href="/">
                <img src="~/Content/images/Logo/libary_logo.png" alt="Library Logo" />
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarContent">

                <ul class="navbar-nav me-auto ms-3">
                    <li class="nav-item">
                        @Html.ActionLink("Trang chủ", "Index", "Home", null, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Quản lý", "Dashboard", "Admin", null, new { @class = "nav-link" })
                    </li>
                </ul>

                @using (Html.BeginForm("TimKiem", "Home", FormMethod.Get))
                {
                    <div class="search-box">
                        <i class="fa fa-search"></i>
                        <input type="text" name="keyword" placeholder="Tìm kiếm sách, tài liệu..." />
                    </div>
                }
            </div>
        </div>
    </nav>

    <div class="container body-content mt-3">
        @RenderBody()
        <hr />

        <footer class="text-light mt-5 py-4">
            <div class="container">
                <div class="row">

                    <div class="col-md-4">
                        <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                            <img src="~/Content/images/Logo/libary_logo.png"
                                 alt="Logo"
                                 style="height: 50px; width: auto;" />
                        </a>
                        <p class="mt-3">
                            Hệ thống quản lý thư viện – hỗ trợ tra cứu, quản lý sách và thông tin độc giả.
                            Phục vụ nhu cầu học tập, giảng dạy và nghiên cứu.
                        </p>
                    </div>

                    <div class="col-md-4">
                        <h5 class="fw-bold mb-3">Liên kết nhanh</h5>
                        <a class="nav-link p-0 mb-2" href="@Url.Action("Index", "Home")">Trang chủ</a>
                        <a class="nav-link p-0 mb-2" href="@Url.Action("Dashboard", "Admin")">Quản trị</a>
                        <a class="nav-link p-0" href="@Url.Action("LienHe", "Home")">Liên hệ</a>
                    </div>

                    <div class="col-md-4">
                        <h5 class="fw-bold mb-3">Thông tin liên hệ</h5>
                        <p><i class="fas fa-map-marker-alt"></i> 140 Lê Trọng Tấn, Tân Phú, TP. Hồ Chí Minh</p>
                        <p><i class="fas fa-phone"></i> 0123 456 789</p>
                        <p><i class="fas fa-envelope"></i> support@library.com</p>

                        <div class="social-links mt-3">
                            <a href="#" class="text-light me-3"><i class="fab fa-facebook fa-2x"></i></a>
                            <a href="#" class="text-light me-3"><i class="fab fa-youtube fa-2x"></i></a>
                            <a href="#" class="text-light"><i class="fab fa-tiktok fa-2x"></i></a>
                        </div>
                    </div>

                </div>
            </div>
        </footer>

    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
